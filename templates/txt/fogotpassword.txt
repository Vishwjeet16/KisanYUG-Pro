<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />

  <meta name="viewport"
        content="width=device-width, initial-scale=1.0"/>

  <title>KisanYUG | Forgot Password</title>

  <!-- Tailwind -->
  <script src="https://cdn.tailwindcss.com"></script>

  <!-- Iconify -->
  <script src="https://code.iconify.design/iconify-icon/1.0.8/iconify-icon.min.js"></script>

  <!-- Google Fonts -->
  <link rel="preconnect"
        href="https://fonts.googleapis.com">

  <link rel="preconnect"
        href="https://fonts.gstatic.com"
        crossorigin>

  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Playfair+Display:wght@600;700&display=swap"
        rel="stylesheet">

  <style>

    *{
      margin:0;
      padding:0;
      box-sizing:border-box;
    }

    body{
      font-family:'Inter',sans-serif;
      background:#f5f5dc;
      overflow-x:hidden;
    }

    .serif{
      font-family:'Playfair Display', serif;
    }

    :root{
      --moss:#4F5D2F;
      --dark:#2D3B2D;
      --oatmeal:#F5F5DC;
      --terracotta:#E2725B;
    }

    .glass{
      background:rgba(255,255,255,0.78);
      backdrop-filter:blur(14px);
      border:1px solid rgba(255,255,255,0.3);
    }

    .hero-bg{
      background:
      linear-gradient(rgba(0,0,0,0.45),
      rgba(0,0,0,0.45)),
      url('https://images.unsplash.com/photo-1501004318641-b39e6451bec6?q=80&w=1600&auto=format&fit=crop');

      background-size:cover;
      background-position:center;
    }

    .input-box{
      transition:0.3s;
    }

    .input-box:focus{
      transform:translateY(-2px);

      box-shadow:0 10px 25px
      rgba(79,93,47,0.15);
    }

    .btn-hover{
      transition:0.3s;
    }

    .btn-hover:hover{
      transform:translateY(-2px);
    }

  </style>

</head>

<body class="min-h-screen hero-bg flex items-center justify-center p-5">

  <!-- Back -->
  <a href="./login.html"
     class="fixed top-6 left-6 z-50 flex items-center gap-2 bg-white/20 hover:bg-white/30 text-white px-5 py-3 rounded-full backdrop-blur-md transition">

    <iconify-icon icon="lucide:arrow-left"></iconify-icon>

    Back

  </a>

  <!-- CARD -->
  <div class="w-full max-w-xl glass rounded-[35px] shadow-2xl p-10">

    <!-- Logo -->
    <div class="flex items-center justify-center gap-3 mb-8">

      <iconify-icon
        icon="lucide:sprout"
        class="text-4xl text-[var(--moss)]">
      </iconify-icon>

      <h1 class="serif text-4xl font-bold text-[var(--moss)]">
        KisanYUG
      </h1>

    </div>

    <!-- Heading -->
    <div class="text-center mb-10">

      <h2 class="serif text-5xl text-gray-800 mb-4">
        Forgot Password
      </h2>

      <p class="text-gray-500 leading-relaxed">
        Enter your registered email address.
        We’ll send you a password reset link.
      </p>

    </div>

    <!-- FORM -->
    <form id="forgotForm"
          class="space-y-6">

      <!-- EMAIL -->
      <div>

        <label class="block text-sm font-semibold text-gray-600 mb-2">
          Email Address
        </label>

        <div class="relative">

          <iconify-icon
            icon="lucide:mail"
            class="absolute left-4 top-1/2 -translate-y-1/2 text-gray-400 text-lg">
          </iconify-icon>

          <input
            type="email"
            id="email"
            required
            placeholder="you@example.com"
            class="input-box w-full pl-12 pr-4 py-4 rounded-2xl border border-gray-200 bg-white outline-none focus:ring-2 focus:ring-[#4F5D2F]"
          >

        </div>

      </div>

      <!-- BUTTON -->
      <button
        type="submit"
        class="w-full bg-[var(--moss)] text-white py-4 rounded-2xl text-lg font-semibold shadow-xl hover:bg-[#2D3B2D] btn-hover">

        Send Reset Link

      </button>

    </form>

    <!-- Bottom -->
    <p class="text-center text-gray-500 mt-8">

      Remember your password?

      <a href="./login.html"
         class="text-[var(--moss)] font-semibold hover:underline">

        Login

      </a>

    </p>

  </div>

  <!-- SCRIPT -->
  <script>

    const forgotForm =
    document.getElementById(
      "forgotForm"
    );

    forgotForm.addEventListener(
      "submit",
      async (e)=>{

        e.preventDefault();

        const email =
        document.getElementById(
          "email"
        ).value;

        alert(
          "Password reset link sent to: "
          + email
        );

      }
    );

  </script>

</body>
</html>